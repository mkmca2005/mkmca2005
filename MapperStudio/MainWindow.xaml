<Window x:Class="MapperStudio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:MapperStudio.ViewModels"
        Title="Mapper Studio" Height="900" Width="1400"
        Background="Transparent"
        FontFamily="{StaticResource AppFontFamily}"
        Foreground="{StaticResource AppForegroundBrush}"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True">
    <Window.DataContext>
        <StaticResource ResourceKey="MainViewModel" />
    </Window.DataContext>
    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource AppForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="{StaticResource AppBackgroundBrush}" CornerRadius="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                <Grid Margin="16,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="28" Height="28" CornerRadius="6" Background="{StaticResource AccentBrush}" Margin="0,0,10,0">
                            <Path Data="M6,8 L22,8 L22,20 L6,20 Z M9,11 L19,11 L19,17 L9,17 Z"
                                  Fill="{StaticResource AppBackgroundBrush}"
                                  Stretch="Uniform" />
                        </Border>
                        <TextBlock Text="Mapping Studio" Style="{StaticResource SectionTitleTextStyle}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource TitleBarButtonStyle}"
                                Command="{x:Static SystemCommands.MinimizeWindowCommand}"
                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                                Content="_" />
                        <Button Style="{StaticResource TitleBarButtonStyle}"
                                Command="{x:Static SystemCommands.MaximizeWindowCommand}"
                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                                Content="□" />
                        <Button Style="{StaticResource TitleBarButtonStyle}"
                                Command="{x:Static SystemCommands.CloseWindowCommand}"
                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                                Content="✕" />
                    </StackPanel>
                </Grid>
            </Border>

            <ContentControl Grid.Row="1" Content="{Binding CurrentView}">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type viewModels:SetupViewModel}">
                        <Grid Margin="24,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="20" Height="20" CornerRadius="10" BorderBrush="{StaticResource AccentBrush}" BorderThickness="2" Margin="0,0,8,0">
                                    <TextBlock Text="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                                </Border>
                                <TextBlock Text="Setup" Style="{StaticResource SubtleTextStyle}" Margin="0,0,12,0" />
                                <TextBlock Text=">" Style="{StaticResource SubtleTextStyle}" Margin="0,0,12,0" />
                                <Border Width="20" Height="20" CornerRadius="10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="2" Margin="0,0,8,0">
                                    <TextBlock Text="2" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                                </Border>
                                <TextBlock Text="Map Fields" Style="{StaticResource SubtleTextStyle}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Save Draft" Style="{StaticResource GhostButtonStyle}" Margin="0,0,12,0" Command="{Binding SaveDraftCommand}" />
                                <Button Content="Next: Map Fields" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding NextCommand}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewModels:ViewModelBase}">
                        <Grid />
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>

            <ContentControl Grid.Row="2" Content="{Binding CurrentView}" Margin="24" />

            <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="24,10">
                <ContentControl Content="{Binding CurrentView}">
                    <ContentControl.Resources>
                        <DataTemplate DataType="{x:Type viewModels:SetupViewModel}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse Width="8" Height="8" Fill="{StaticResource SuccessBrush}" Margin="0,0,8,0" />
                                    <TextBlock Text="{Binding ConnectionStatusText}" Style="{StaticResource SubtleTextStyle}" Margin="0,0,16,0" />
                                    <TextBlock Text="Last validation:" Style="{StaticResource SubtleTextStyle}" Margin="0,0,6,0" />
                                    <TextBlock Text="{Binding LastValidationTime}" Style="{StaticResource SubtleTextStyle}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="ENV:" Style="{StaticResource SubtleTextStyle}" Margin="0,0,6,0" />
                                    <TextBlock Text="{Binding EnvironmentText}" Style="{StaticResource SubtleTextStyle}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type viewModels:ViewModelBase}">
                            <Grid />
                        </DataTemplate>
                    </ContentControl.Resources>
                </ContentControl>
            </Border>
        </Grid>
    </Border>
</Window>
