<UserControl x:Class="MapperStudio.Views.MappingEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="RowStatusPillStyle" TargetType="Border" BasedOn="{StaticResource StatusPillStyle}">
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding StatusSeverity}" Value="Success">
                    <Setter Property="Background" Value="{StaticResource SuccessBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource SuccessBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding StatusSeverity}" Value="Warning">
                    <Setter Property="Background" Value="{StaticResource WarningBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding StatusSeverity}" Value="Error">
                    <Setter Property="Background" Value="{StaticResource ErrorBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource ErrorBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Text="Field Mapping" Style="{StaticResource PageTitleTextStyle}" />
            <TextBlock Text="SalesForce Leads to Azure SQL Production" Style="{StaticResource SubtleTextStyle}" />
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0,16,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Style="{StaticResource WatermarkTextBoxStyle}" Tag="Search mappings..." />
                <Button Grid.Column="1" Content="Filters" Style="{StaticResource GhostButtonStyle}" Margin="12,0,0,0" />
                <Button Grid.Column="3" Content="Auto-Match" Style="{StaticResource GhostButtonStyle}" Margin="12,0,0,0" />
            </Grid>
        </Border>
        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
            <DataGrid ItemsSource="{Binding MappingRows}"
                      AutoGenerateColumns="False"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="" Width="40">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox VerticalAlignment="Center" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Target Field" Width="220">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding TargetField}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding TargetType}" Style="{StaticResource SubtleTextStyle}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridComboBoxColumn Header="Source Field (JSON Path)" Width="240"
                                            SelectedItemBinding="{Binding SelectedSourceField}"
                                            ItemsSource="{Binding DataContext.SourceFields, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                    <DataGridComboBoxColumn Header="Strategy" Width="160"
                                            SelectedItemBinding="{Binding SelectedStrategy}"
                                            ItemsSource="{Binding DataContext.Strategies, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                    <DataGridTemplateColumn Header="Config" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="⚙" Style="{StaticResource IconButtonStyle}" Command="{Binding ConfigureCommand}" Width="32" Height="32" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Status" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource RowStatusPillStyle}">
                                    <TextBlock Text="{Binding StatusLabel}" Foreground="{StaticResource AppBackgroundBrush}" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
